{
  "name": "sv-home-products",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["shared", "frontend", "backend"],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w shared && npm run build -w frontend",
    "lint": "npm run lint -w shared && npm run lint -w frontend && npm run lint -w backend",
    "clean": "rm -rf frontend/dist backend/dist shared/dist"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  }
}
